/* import { GET_QUESTIONS} from '../constants/actionTypes';

const questionsReducer = (questions = { questionsData: null}, action) => {
    switch (action.type) {
        case GET_QUESTIONS:
            return { ...questions, questionsData:action?.payload }
        default:
            return questions;
    }
}

export default questionsReducer; */